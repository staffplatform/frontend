export enum ERouter {
    AUTH = "/auth",
    REGISTER = "/register",
    PROFILE = "/profile",
    SCHEDULE = "/schedule",
    ORGANIZATION = "/organization",
    ORGANIZATION_STORES = "/organization/stores",
    ORGANIZATION_EMPLOYEES = "/organization/employees"
}
